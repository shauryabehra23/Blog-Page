# Backend collaboratorControllers.js Fixes

## Steps:

- [ ] 1. Fix duplicate `to: [email]` in sendCollaborationInvites resend.emails.send
- [ ] 2. Fix `user: blogOwner` → `user: blogOwner._id` in collab creation
- [ ] 3. Fix email `from` field formatting (remove extra backticks)
- [ ] 4. Consolidate duplicate code between sendCollaborationInvites and sendInvite
- [ ] 5. Test endpoints

Status: Implementing fixes...
